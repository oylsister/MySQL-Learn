-- LESSON 1 DATABASE.

-- Create Database
CREATE DATABASE JameDB;

-- Use database as default
USE JameDB;

-- Delete Database
DROP DATABASE JameDB;

-- Change to read only, When change to read only you can't delete it.
-- To change back set READ ONLY to 0.
ALTER DATABASE JameDB READ ONLY = 1;

